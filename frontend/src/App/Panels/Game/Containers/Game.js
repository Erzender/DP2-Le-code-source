import { connect } from 'react-redux'
import GameCpt from '../Components/GameCpt'

const mapStateToProps = (state) => ({
})

const mapDispatchToProps = (dispatch) => ({
  
})

const Game = connect(
  mapStateToProps,
  mapDispatchToProps
)(GameCpt)

export default Game
